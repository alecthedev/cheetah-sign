## Known issues
- UI/UX is not the same as client uses internally (No official inverted Cheetah Logo)
- LibreOffice dependency causes Docker image to build slowly
- Frontend tests can take awhile to run
- Still serves pages locally

## Improvements / Suggestions for Future development
- Actually host the pages so others can remotely sign (ngrok?) 
- Doc conversion with lighter dependency 
- Create some subdivisions of tests to run subsets at a time, to speed up development 
- Incorporate more of Cheetah's in-house UI/UX 
- Create a company email to use for sending docs (written in .env) 

